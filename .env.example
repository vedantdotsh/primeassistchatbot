# Required API keys
GROQ_API_KEY=your_groq_api_key_here

# Optional model/configuration settings
MODEL_NAME=
FLASK_ENV=development
PORT=5000

# Add any project-specific secrets here, but keep real values only in .env
